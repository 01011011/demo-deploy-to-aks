apiVersion: v1
kind: Service
metadata:
  name: de
  namespace: default
  labels:
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app: de
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80